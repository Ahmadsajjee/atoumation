name: Hello World Workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Run a one-line script
      run: echo Hello, World! ðŸŽ‰

    - name: Run a multi-line script
      run: |
        echo "This is a GitHub Actions workflow"
        echo "It runs on every push to main"
        echo "Check the Actions tab to see this running!"
